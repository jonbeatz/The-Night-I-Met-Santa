# Images/

| Subfolder | Role |
|-----------|------|
| **`references/`** | Photos + Christmas book refs + **`layout/`** north stars |
| **`chopz/`** | Exports for InDesign (MOCK, LEFT/RIGHT, textCloud, paintFrame) |

**Not here:** generated book art → `Media/generated/` · approved keepers → `Media/approved/`.  
Scratch (`ai/`, `test-images/`, `jack/`) parked **2026-07-20** → `_archive/images-scratch/`.
